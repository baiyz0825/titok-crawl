services:
  douyin-scraper:
    build: .
    container_name: douyin-scraper
    ports:
      - "8000:8000"   # Web UI + API
      - "8001:8001"   # MCP SSE
    volumes:
      - ./data:/app/data
    environment:
      - HEADLESS=true
    restart: unless-stopped
